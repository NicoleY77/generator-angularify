language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angularify' ]; then cd .. && eval "mv $currentfolder generator-angularify" && cd generator-angularify; fi